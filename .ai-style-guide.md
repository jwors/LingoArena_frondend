# LingoArena Frontend Style Guide

## Design Philosophy
干净、克制、可读。浅色背景 + 单一品牌色，避免装饰性渐变和堆叠动画。

## Color Palette
- Primary (品牌色): violet-600 (#7c3aed)
- Primary Light: violet-500 (#8b5cf6)
- Primary Dark: violet-700 (#6d28d9)
- Success: emerald-600 (#059669)
- Error/Danger: rose-500 (#f43f5e)
- Warning: amber-500 (#f59e0b)
- Background: stone-50 (#fafaf9)
- Card BG: white + border-gray-200
- Text Primary: gray-900 (#111827)
- Text Secondary: gray-500 (#6b7280)
- Text Muted: gray-400 (#9ca3af)
- Border: gray-200 (#e5e7eb)

## Typography
- 标题字体: Outfit (`font-display` 或 h1/h2/h3 默认)
- 正文字体: Source Sans 3
- 页面最大宽度: max-w-xl (672px)
- 标题层级: text-3xl (H1), text-xl (H2), text-lg (H3)

## Border Radius
- 卡片/面板: rounded-xl (12px)
- 按钮/输入框: rounded-lg (8px)
- 标签/徽章: rounded-md (6px)
- 头像: rounded-full

## Shadows
- 尽量不用阴影，用 border 分隔
- Modal 等浮层可用 shadow-lg

## Components Pattern
- 卡片: `.card` = bg-white + rounded-xl + border + p-6
- 主按钮: `.btn-primary`
- 输入框: `.input-field`
- Logo: 使用 `<Logo />` 组件，禁止渐变文字 Logo
- 面板标题: `.panel-header`，禁止 emoji + 渐变图标盒

## Animations
- 保留: Toast 入场、答题反馈 (ResultFeedback)、Modal、loading spin
- 禁止: 页面级 fade-in/slide-up 堆叠、pulse-glow、blur 光斑背景

## DO
- 使用 `<Logo />` 统一品牌标识
- 用 border 和留白区分层级
- 词库选择等功能性 emoji 可保留

## DON'T
- 不要使用 violet/sky 双渐变背景或渐变文字
- 不要使用 blur-3xl 装饰光斑
- 不要使用 indigo 色系（旧色）
- 不要在每个卡片上加 animate-fade-in / animate-slide-up
- 不要用 emoji 充当面板图标（🏠 🚪 ⚔️ 等）
